@model IEnumerable<eRNI.Models.ProjectCategory>

<li class="dropdown-submenu">
        <a href="#">Kategoria projektu</a>
        <ul class="dropdown-menu">
           @foreach (var item in Model)
           {
            <li><a href="@Url.Action("ProjectCategory", "MenuFilters", new { id = item.projectCategryName })">@item.projectCategryName</a></li>
           }
        </ul>
</li>

